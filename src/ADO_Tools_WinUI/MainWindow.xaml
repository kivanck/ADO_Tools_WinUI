<Window
    x:Class="ADO_Tools_WinUI.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ADO_Tools_WinUI"
    xmlns:pages="using:ADO_Tools_WinUI.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="ADO Tools">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Top command bar with Settings button -->
        <Grid Padding="16,8" Background="{ThemeResource LayerFillColorDefaultBrush}">
            <TextBlock Text="ADO Tools"
                       Style="{StaticResource TitleTextBlockStyle}"
                       VerticalAlignment="Center" />
            <Button x:Name="btnSettings"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    ToolTipService.ToolTip="Settings"
                    Click="BtnSettings_Click">
                <SymbolIcon Symbol="Setting" />
            </Button>
        </Grid>

        <!-- Tab layout keeps both pages alive simultaneously -->
        <TabView Grid.Row="1"
                 x:Name="MainTabView"
                 IsAddTabButtonVisible="False"
                 CanDragTabs="False"
                 CanReorderTabs="False"
                 TabWidthMode="SizeToContent">

            <TabViewItem Header="Work Items" IsClosable="False">
                <TabViewItem.IconSource>
                    <SymbolIconSource Symbol="Library" />
                </TabViewItem.IconSource>
                <pages:WorkItemsPage />
            </TabViewItem>

            <TabViewItem Header="Software Download" IsClosable="False">
                <TabViewItem.IconSource>
                    <SymbolIconSource Symbol="Download" />
                </TabViewItem.IconSource>
                <pages:SoftwareDownloadPage />
            </TabViewItem>

        </TabView>
    </Grid>
</Window>
